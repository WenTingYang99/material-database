# 素材库项目框架（just_html 分支）

> 最后核对日期：2026-07-14
> 定位说明：当前工作区是 DPCA（神龙汽车）素材库的**纯前端静态原型**，用 `localStorage` 模拟业务数据，不依赖后端。`DATABASE_SCHEMA.md` 与 `MERGE_ANALYSIS.md` 是**面向未来的后端落地规划文档**，描述把这套前端接成真实关系库（并并入 DPCA-AI 创意平台）时的表结构与融合策略，**当前代码尚未实现**。

## 页面与样式

- 项目是纯前端静态应用，入口为 `index.html`，通过浏览器 `localStorage` 持久化模拟业务数据（`STORAGE_KEY = "dp-material-library-state-v2"`）。
- 全局样式集中在 `styles.css`，保留现有类名和视觉规则，新增页面优先复用现有按钮、表单、弹窗、卡片、表格和 `dp-` 前缀反馈组件。
- 登录后默认进入 `首页`，顶部一级菜单包含 `首页`、`素材管理`、`更多功能`。菜单是否展示由菜单权限和值列表状态共同控制，首页入口对已登录用户默认可见，首页内容继续按素材和素材组权限过滤。
- 主布局包含顶部栏、左侧素材组树、工作区、素材工具栏、素材列表/卡片、首页信息卡片、预览器、素材篮、管理页和多个业务弹窗。
- 首页展示素材热榜、最新素材列表和用户配置的素材组列表；所有素材条目都必须先通过 `canViewAsset(asset)`，素材组配置项必须先通过 `canViewGroup(groupId)`。**注意：`getHomeVisibleAssets()` 目前只判断 `asset.assetStatus`（无 `status` 兜底），种子素材因缺 `assetStatus` 会在首页不显示，属已知问题（见 CODE_MEMORY）。**
- 主题支持浅色和深色，通过 `body[data-theme="dark"]` 切换。
- 公共提示、确认弹窗、Loading、外部门户密码门和上传进度提示均沿用统一组件风格。

## 页面注册

- 当前项目不是多 HTML 页面结构，新增页面不新增独立 `.html` 文件，而是在菜单数据、页面状态和渲染分发中注册新的 SPA 页面。
- 已新增 `home` 页面：默认菜单节点为 `menu_home`，页面名称为 `首页`，布局标识为 `home`，登录成功后默认进入该页面。
- `home` 页面由 `render()` 分发到 `renderHomePage()`，页面内容包含素材热榜、最新素材列表和素材组展示配置。
- `ensureSystemData()` 会给已有本地数据补齐缺失的默认菜单节点，包括 `menu_home`，避免老缓存中没有首页入口。
- 顶部/侧边导航由 `db.menus` 动态渲染；后续新增业务页面应继续遵循“菜单节点 + page 编码 + render 分发 + 权限配置”的方式。

## 核心功能

- 首页：素材热榜按访问量排序展示当前用户可查看的前 8 条素材；最新素材按上传/创建时间排序展示当前用户可查看的前 8 条素材；素材组展示配置支持在有权限的素材组树中最多选择 4 个素材组，并在首页以卡片列表展示对应组的最新素材。
- 素材浏览：全部素材、待入库、我创建的组、素材组内素材、回收站和更多管理页。
- 素材展示：卡片视图、列表视图、日期分组、预览悬浮、详情预览器。
- 搜索筛选：全局搜索、常用筛选、上传日期筛选、排序、筛选配置；下拉、多选和筛选候选优先来自值列表，界面展示名称，业务存储和判断使用 code 或稳定字段（实际运行中部分枚举仍存展示串，见 CODE_MEMORY 已知问题）。
- 素材组：新增、编辑、移动、排序、复制、解散、删除、恢复；素材组权限采用 ACL 继承模型，包含可见、下载、素材维护和组管理。
- 上传导入：内部上传、本地文件夹、云导入入口、外部收集上传。内部上传会自动创建一个系统收集任务，素材进入待审核/待入库链路。
- 时间选择：素材生效/失效、分享过期、收集任务截止/失效等使用日期 + 时间控件，日期选择参考 flatpickr 交互，时间字段只允许选择或控件输入。
- 素材篮：批量选择、删除、下载、修改有效期、分享。
- 管理页：用户动态、用户登录日志、标签管理、值列表管理、菜单管理、权限管理、有效期管理、收集素材、分享记录、回收站。
- 分享与收集：支持 `?share=` 分享页和 `?collect=` 收集页。分享页支持访问范围、内容权限、访问密码、有效期和访问次数限制；收集页支持密码门、素材上传、暂存、提交待入库、审核历史弹窗和进度查看。

## 权限体系

- 菜单权限控制页面入口和可编辑状态，来源包含角色、组织和用户个人权限；`getMenuPermission()`、`canViewMenu()`、`canEditMenu()` 是页面级入口判断。
- 首页入口默认对登录用户可见，但首页中的素材热榜、最新素材和素材组展示必须经过素材/素材组数据权限过滤。
- 素材组权限使用 `db.groupAcl`，支持人员、部门、公司主体，权限向父级组继承；`canViewGroup()`、`canDownloadFromGroup()`、`canContributeToGroup()`、`canManageGroup()` 是主要判断函数。
- 素材权限使用 `db.assetAcl` 作为素材级补充授权；素材查看、编辑、下载、删除、恢复和彻底删除分别由 `canViewAsset()`、`canEditAsset()`、`canDownloadAsset()`、`canDeleteAsset()`、`canRestoreAsset()`、`canPurgeAsset()` 控制。
- 收集任务管理由 `canManageCollect(task)` 控制；创建或配置收集任务时，存放素材组只能选择当前用户有素材维护或组管理权限的素材组，也允许不指定素材组。
- 分享创建与管理由 `canCreateShare(target, permission)` 和 `canManageShare(share)` 控制，分享落地页还会叠加访问范围、密码、有效期、访问次数和内容权限。

## 值列表

- 值列表是业务枚举的统一来源，覆盖素材来源、文件格式、素材状态、审核状态、分享范围、分享内容权限、收集任务状态、收集审核动作、审核备注、业务标签等。
- 文件格式值列表包含 `其他`。收集任务选择 `其他` 时表示不限格式，浏览器文件选择器不限制 `accept`，提交时也不拦截具体扩展名。
- 业务标签来自 `db.tags`（与值列表树是独立体系），外部收集页支持多选和新增，提交到素材时写入素材业务标签。
- 审核历史中的操作使用稳定动作 code，界面展示中文动作名称；备注优先使用审核备注值列表，其他场景允许审核人自定义输入。

## 收集与审核流程

- 新建收集任务时填写任务主题、说明、提交人信息要求、存放素材组、截止时间、允许上传的文件类型；存放素材组可为空，也可从当前用户有维护或管理权限的素材组树中选择。
- 收集任务初始可暂存。暂存时任务和素材处于待提交状态；提交到待入库后进入待审核状态。
- 只有待提交状态可以继续访问收集链接并修改、暂存、提交。待审核或审核中状态再次访问链接时，页面提示当前收集任务已发起审核，请等待审核结果。
- 提交后，收集素材管理页显示机审和审核按钮；点击机审或人审后，状态进入对应审核链路。
- 审核通过后素材变为已入库；审核驳回后对应素材回到待提交/驳回后可修改状态，提交人可以继续通过收集链接修改并再次提交，同一批素材尽量沿用原素材记录更新状态。
- 收集页和管理页都提供“查看审核历史”按钮，点击后以弹框展示当前收集任务的进度记录，包含人、时间、动作和备注。
- 内部上传为了统一查看状态，会由系统自动创建一个内部收集任务，允许上传全部素材。用户点击提交后，素材直接进入待审核状态，并在待入库页面统一处理。

## 脚本拆分

- `app-infra.js`：公共基础设施，包含 Toast、Modal.confirm、http、Loading、debounce、日期/时间选择锁定和 flatpickr 初始化。
- `app-core.js`：初始数据、值列表、权限函数、全局状态、DOM 引用、启动函数、数据归一化（`normalizeDbDates`/`normalizeDbSelectionCodes`/`normalizeStoredTagCodes` 等）、分享/收集独立页、运行时元素、登录/会话。
- `app-render.js`：主渲染、首页、素材列表、管理页、标签页、筛选、素材组树、首页素材组配置弹窗。
- `app-actions.js`：内部上传、素材组操作、菜单、预览器、素材编辑、协作、标签管理、收集任务创建表单。
- `app-workflows.js`：素材篮、分享、收集任务配置、收集任务审核、通用表单、素材权限、下载删除、权限申请审批等工作流。
- `app-utils.js`：素材过滤、排序、相似度、日期时间格式化、文件类型校验、HTML 转义、整库日期归一化等工具函数。
- `app.js`：最后加载，只调用 `bootstrap()` 启动应用。

## 数据

- 数据存储在浏览器 `localStorage`，主键为 `dp-material-library-state-v2`；首页素材组展示配置按用户单独存储在 `dp-material-library-home-groups:{userId}`。
- 主要业务数据结构包括 `groups`、`assets`、`collectTasks`、`shares`、`tags`、`valueListTree`、`menus`、`roles`、`organizations`、`users`、`groupAcl`、`assetAcl`、`operationLogs`、`permissionRequests`。
- 素材状态使用 `assetStatus` 表示资产主状态，使用 `auditStatus` 表示审核状态；待入库口径统一由 `isPendingAsset(asset)` 判断。**当前种子数据只写了 `status:"active"`，未带 `assetStatus`/`auditStatus`，属已知缺陷。**
- 收集任务记录 `groupId` 和展示用组名；`types` 保存允许上传的文件类型名称，其中 `其他` 表示不限格式。
- 分享记录保存访问范围、内容权限、密码、有效期、最大访问次数、目标类型和目标 ID。
- 操作记录写入实体日志和全局 `operationLogs`，时间统一按 `YYYY-MM-DD HH:mm:ss` 或业务页面需要的分钟级格式展示。

## 启动与缓存

- 静态资源通过 `index.html` 中的 `?v=` 参数控制浏览器缓存刷新；调整脚本或样式后需要同步更新版本标记。
- 若浏览器仍展示旧交互，优先执行强制刷新或清理站点缓存，不在业务代码中加入自动清理历史草稿或历史数据的隐藏函数。

## 后端规划文档关系

- `DATABASE_SCHEMA.md`：素材库**自身**的关系型表结构设计（目标态），共 19 张表，含字段级定义、ER 图与前端字段对照。是后端落地的“数据模型基准”。
- `MERGE_ANALYSIS.md`：把这些表**并入 DPCA-AI 创意平台（develop）现有库**的融合分析报告（部署态），逐表给出“新增 / 替换 / 扩展 / 合并”策略并给出 SmartAdmin 命名下的建表 DDL。它直接建立在 `DATABASE_SCHEMA.md` 的 19 表清单之上。
- 二者不是同一份文档：前者回答“素材库需要哪些表”，后者回答“这些表怎么融入现有平台”。详情见两份文档顶部的关系说明。
