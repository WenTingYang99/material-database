# 素材库项目基线

## 页面与样式

- 页面为纯前端静态应用，入口是 `index.html`。
- 全局样式在 `styles.css`，保留现有类名和视觉规则，不做样式重命名。
- 主布局包含顶部栏、左侧素材组树、工作区、素材工具栏、素材列表/卡片、预览器、素材篮、管理页和多个弹窗。
- 主题支持浅色和深色，通过 `body[data-theme="dark"]` 切换。
- 公共提示、确认弹窗和 Loading 使用 `dp-` 前缀样式。

## 核心功能

- 素材浏览：全部素材、待入库、我创建的组、更多管理页。
- 素材展示：卡片视图、列表视图、日期分组、预览悬浮、详情预览器。
- 搜索筛选：全局搜索、常用筛选、上传日期筛选、排序、筛选配置。
- 素材组：新增、编辑、移动、排序、收藏、复制、解散、删除、恢复。
- 上传导入：本地文件、文件夹、云导入入口、外部收集上传。
- 时间选择：素材生效/失效、分享过期、收集任务失效等都使用日期 + 时间选择控件，只能选择不能手输。
- 素材篮：批量选择、删除、下载、修改有效期、分享。
- 管理页：用户动态、标签管理、有效期管理、收集素材、分享记录、回收站。
- 分享与收集：支持 `?share=` 分享页和 `?collect=` 收集页。

## 脚本拆分

- `app-infra.js`：公共基础设施，包含 Toast、Modal.confirm、http、Loading、debounce、日期/时间选择锁定。
- `app-core.js`：初始数据、状态、DOM 引用、启动函数、数据加载和迁移、分享/收集独立页、运行时元素。
- `app-render.js`：主渲染、素材列表、管理页、标签页、筛选、素材组树。
- `app-actions.js`：上传、素材组操作、菜单、预览器、素材编辑、协作、标签管理弹窗。
- `app-workflows.js`：素材篮、分享、收集任务、通用表单、素材权限和下载删除等工作流。
- `app-utils.js`：素材过滤、排序、相似度、日期时间、格式化、HTML 转义等工具函数。
- `app.js`：最后加载，只调用 `bootstrap()` 启动应用。

## 数据

- 数据存储在浏览器 `localStorage`，键为 `dp-material-library-state-v2`。
- 主要数据结构：`groups`、`assets`、`collectTasks`、`shares`。
- 当前用户为前端模拟用户 `Kerry/admin`。
