# 权限体系与收集分享流程分析报告

> 分析日期：2026-07-10
> 分析基准：当前本地工作区
> 覆盖范围：权限体系、值列表、素材入库、收集任务、分享落地页、菜单管理

---

## 一、当前巡检结论

### 1.1 已闭环的核心问题

| 原问题 | 当前实现 |
|---|---|
| 素材权限弹窗调用未定义的 `getEffectiveGroupPermissionLevel()` | `app-core.js` 已定义 `getEffectiveGroupPermissionLevel(groupId)`，素材权限弹窗可展示继承的组权限等级。 |
| 收集任务配置模板的日期占位符未渲染 | 收集任务配置和分享记录配置均通过 `renderHtmlTemplate()` 注入 `EXPIRE_DATE`、`EXPIRE_TIME`、`REQUIRED_ATTR`，静态模板中的占位符不会直接展示给用户。 |
| 素材状态迁移破坏审核链路 | 当前主状态使用 `assetStatus`，审核状态使用 `auditStatus`；上传、外部收集、机审、人审、入库和删除均围绕这两个字段流转。 |
| 素材组发起收集时把 groupId 当组名显示 | 组菜单和当前组选中态均调用 `getGroupName(groupId)` 后再打开收集任务弹窗。 |
| `getTreeNodeByCode("view")` 跨维度冲突 | `getTreeNodeByCode(code, parentDimCode)` 支持维度限定；组权限、素材权限、分享权限显示分别传入对应维度。 |
| 权限申请提交后无审批入口 | 权限管理页已接入申请列表、状态筛选、通过和拒绝按钮，按钮调用 `approvePermissionRequest()` / `rejectPermissionRequest()`。 |
| 组/素材 ACL 管理弹窗不可达 | 组右键菜单可打开 `openGroupPermissionModal()`；素材查看器可打开 `openAssetPermissionModal()`。 |
| 分享弹窗重复绑定事件 | 分享有效期按钮绑定前使用 `cloneNode(true)` 替换旧节点，避免重复监听器累积。 |
| 内部门户登录后权限缓存陈旧 | 分享内部门户登录成功后会重建 `currentUser` 并调用 `buildUserGroupPermissionCache()`。 |
| 分享访问次数先展示再过期 | `renderSharePortalContent()` 先计算 `nextVisits` 并在超限时直接拦截，未超限才写入访问次数。 |
| 上传素材直接进入素材库 | `handleFiles()` 和外部收集上传均传入待审核 `auditStatus` 与待入库 `assetStatus`；`createAssetFromFile()` 默认也落到待入库。 |

### 1.2 本轮修正后的权限和值列表口径

- 待入库统一由 `isPendingAsset(asset)` 判断，覆盖 `assetStatus=pending` 以及 `auditStatus` 未终审通过的素材；`getFilteredAssets()`、`renderAssets()`、`approveSelectedAssets()` 已复用同一口径。
- ACL 主体匹配统一由 `aclMatchesCurrentUser(acl)` 处理，支持 `user`、`department`、`company`；部门授权支持 `includeSubDept` 通过组织祖先链匹配。
- 组 ACL 和素材 ACL 弹窗支持公司级授权，`subjectId="all"` 表示全公司；切换主体类型会清空旧主体，避免提交脏数据。
- 权限申请审批只处理 `pending` 状态；审批通过时先查找既有 ACL，存在则更新，不存在才新增，避免重复点击产生重复授权。
- 值列表编码唯一性按同父节点约束，允许不同维度复用 `active`、`enabled`、`view` 等通用枚举码；编辑节点时禁止移动到自身子孙节点。
- 菜单管理对 `page` 做唯一校验；删除菜单树时同步清理角色、组织、用户权限对象中的残留页面 key，并规范当前页面状态。

### 1.3 仍需关注的维护项

| 项目 | 当前判断 | 建议 |
|---|---|---|
| 旧兼容函数与模板 | `revokeShare()`、`closeCollectTask()` 等函数仍作为兼容能力保留，部分旧模板也还在 `index.html` 中 | 后续按页面确认是否仍被动态模板或旧入口依赖，再分批清理。 |
| 前端权限边界 | 当前权限体系主要用于前端显隐、流程约束和本地数据校验 | 引入后端后，需要把 `isPendingAsset()`、`aclMatchesCurrentUser()`、菜单权限和审批状态判断同步到服务端。 |
| 部门授权交互 | ACL 数据支持 `includeSubDept`，组权限弹窗已有包含子部门选项；素材权限弹窗更偏轻量 | 若业务需要精细控制素材级部门继承，可给素材 ACL 弹窗也补同样的显式开关。 |

---

## 二、完整权限体系：能力与限制

### 2.1 权限体系架构总览

```
┌─────────────────────────────────────────────────────┐
│                    权限体系                           │
├──────────────┬──────────────┬───────────────────────┤
│  菜单权限     │  素材组权限   │     素材权限           │
│ (db.menus)   │ (db.groupAcl) │  (db.assetAcl)       │
├──────────────┼──────────────┼───────────────────────┤
│ visible      │ view(可见)    │ view(仅预览)          │
│ editable     │ download(下载)│ edit(可编辑)          │
│              │ contribute    │ download(可下载)      │
│              │   (素材维护)   │ full(全部权限)        │
│              │ manage(组管理)│                      │
├──────────────┼──────────────┼───────────────────────┤
│ 角色绑定      │ 组继承(向上)  │ 组继承 + 独立授权     │
│ 组织绑定      │ 主体:人/部门/ │ 主体:人/部门/公司     │
│ 个人绑定      │   公司       │ 有效期控制            │
└──────────────┴──────────────┴───────────────────────┘
```

### 2.2 菜单权限（页面访问控制）

**数据结构**：`db.roles[].permissions` / `db.orgPermissions` / `db.userPermissions`

**能力**：
- 每个菜单项有 `visible`（可见）和 `editable`（可编辑）两个布尔属性
- 权限来源优先级：个人权限 > 组织权限 > 角色权限（合并取最高）
- 超级管理员（`super_admin` 角色）拥有全部菜单的 visible+editable
- `canViewMenu(page)` 判断页面是否可见
- `canEditMenu(page)` 判断页面是否可编辑

**限制**：
- 菜单权限只控制页面入口和操作按钮的显隐，不控制数据级别
- 如：有标签菜单 visible 但无 editable，则不显示"编辑"按钮但仍能看到标签数据

### 2.3 素材组权限（ACL 继承模型）

**数据结构**：`db.groupAcl[]`
```javascript
{
  id, groupId, subjectType, subjectId, subjectName,
  permission, includeSubDept, grantedBy, grantedAt
}
```

**权限等级（高包含低）**：

| 等级 | code | 名称 | attr1 | 能力 |
|------|------|------|-------|------|
| 1 | view | 可见 | 1 | 查看素材组及其素材 |
| 2 | download | 下载 | 2 | 可见 + 下载素材原始文件 |
| 3 | contribute | 素材维护 | 3 | 下载 + 上传/编辑/删除素材 |
| 4 | manage | 组管理 | 4 | 素材维护 + 管理组设置和权限 |

**继承机制**：
- 组权限**向上继承**：当前组无直接 ACL 时，沿 `parentId` 链向上查找
- `getGroupEffectiveAcl(groupId)` 遍历父链收集所有有效 ACL
- `hasGroupPermissionLevel(groupId, requiredPerm)` 取用户在所有有效 ACL 中的最高权重
- 缓存机制：`buildUserGroupPermissionCache()` 在 `render()` 时构建 Map，`invalidateGroupPermissionCache()` 在组变更时清除

**授权主体**：
- `user`：指定单个用户（通过 userId 匹配）
- `department`：指定部门（通过 organizationId 匹配）
- `company`：全公司（subjectId="all" 匹配所有人）

**创建素材组时的自动授权**：
- 创建者自动获得 `manage` 权限（`handleAddGroupSubmit` 中写入 `db.groupAcl`）
- `ensureSystemData()` 为已有组补齐创建者的 manage ACL

### 2.4 素材权限（ACL 正交模型）

**数据结构**：`db.assetAcl[]`
```javascript
{
  id, assetId, subjectType, subjectId, subjectName,
  permission, expiresAt, grantedBy, grantedAt
}
```

**权限等级（正交平行，不包含）**：

| 等级 | code | 名称 | 包含的原子权限 |
|------|------|------|---------------|
| 1 | view | 仅预览 | view |
| 2 | edit | 可编辑 | view + edit |
| 3 | download | 可下载 | view + download |
| 4 | full | 全部权限 | view + edit + download |

**权限映射**：
```javascript
const ASSET_PERMISSION_MAP = {
  view: ["view"],
  edit: ["view", "edit"],
  download: ["view", "download"],
  full: ["view", "edit", "download"],
};
```

**继承机制**：
- 素材权限**从组继承**：`canViewAsset` / `canEditAsset` / `canDownloadAsset` 先检查组权限
- 组权限满足则直接通过，不检查素材 ACL
- 组权限不满足时，再检查素材级别的独立 ACL
- 素材 ACL 支持 `expiresAt` 过期时间

**权限判断优先级**：
```
isAdmin() → asset.ownedBy === currentUser.name → 组权限 → 素材ACL → false
```

### 2.5 权限函数完整清单

| 函数 | 文件 | 说明 |
|------|------|------|
| **判断函数** | | |
| `isAdmin()` | app-core.js | 是否超级管理员 |
| `canManageAsset(asset)` | app-core.js | 能否管理素材（admin/owner） |
| `canViewAsset(asset)` | app-core.js | 能否查看素材 |
| `canEditAsset(asset)` | app-core.js | 能否编辑素材 |
| `canDownloadAsset(asset)` | app-core.js | 能否下载素材 |
| `canDeleteAsset(asset)` | app-core.js | 能否删除素材 |
| `canRestoreAsset(asset)` | app-core.js | 能否恢复素材 |
| `canPurgeAsset(asset)` | app-core.js | 能否彻底删除素材 |
| `canMoveAsset(asset, targetGroupId)` | app-core.js | 能否移动素材 |
| `canViewGroup(groupId)` | app-core.js | 能否查看素材组 |
| `canDownloadFromGroup(groupId)` | app-core.js | 能否从组下载 |
| `canContributeToGroup(groupId)` | app-core.js | 能否上传/编辑组内素材 |
| `canManageGroup(groupId)` | app-core.js | 能否管理组设置 |
| `canUploadToGroup(groupId)` | app-core.js | 能否上传到组 |
| `canMoveGroup(src, dst)` | app-core.js | 能否移动组（含循环检测） |
| `canCreateShare(target, perm)` | app-core.js | 能否创建分享 |
| `canManageShare(share)` | app-core.js | 能否管理分享 |
| `canManageTag(tag)` | app-core.js | 能否管理标签 |
| `canManageCollect(task)` | app-core.js | 能否管理收集任务 |
| `isPendingAsset(asset)` | app-core.js | 判断素材是否仍处于待入库/待终审链路 |
| `aclMatchesCurrentUser(acl)` | app-core.js | 统一匹配 user / department / company ACL 主体 |
| `getOrgAncestorIds(orgId)` | app-core.js | 获取当前组织上级链，用于包含子部门授权 |
| `getEffectiveGroupPermissionLevel(groupId)` | app-core.js | 获取当前用户在组继承 ACL 中的最高权限等级 |
| **ACL 管理函数** | | |
| `addGroupAcl()` | app-workflows.js | 添加组 ACL |
| `removeGroupAcl()` | app-workflows.js | 移除组 ACL |
| `updateGroupAcl()` | app-workflows.js | 更新组 ACL |
| `transferGroupOwner()` | app-workflows.js | 转交组责任人 |
| `addAssetAcl()` | app-workflows.js | 添加素材 ACL |
| `removeAssetAcl()` | app-workflows.js | 移除素材 ACL |
| **权限申请函数** | | |
| `openPermissionRequestModal()` | app-workflows.js | 打开权限申请弹窗 |
| `approvePermissionRequest()` | app-workflows.js | 权限管理页审批通过，写入或更新 ACL |
| `rejectPermissionRequest()` | app-workflows.js | 权限管理页审批拒绝 |
| `submitPermissionRequest()` | app-workflows.js | 旧提交入口，保留兼容 |
| **缓存函数** | | |
| `buildUserGroupPermissionCache()` | app-core.js | 构建组权限缓存 |
| `invalidateGroupPermissionCache()` | app-core.js | 清除组权限缓存 |

### 2.6 权限的 UI 控制点

| 场景 | 权限检查 | UI 效果 |
|------|----------|---------|
| 左侧素材组树 | `canViewGroup` | 无权限的组不显示 |
| 素材组操作按钮 | `canManageGroup` | 无权限不显示"…"和"×"按钮 |
| 上传按钮 | `canUploadToGroup` | 无权限隐藏上传按钮 |
| 素材卡片快捷操作 | `canEditAsset` / `canDownloadAsset` / `canCreateShare` | 无权限不显示对应按钮 |
| 素材列表操作按钮 | `canEditAsset` / `canDownloadAsset` / `canShare` / `canDelete` | 无权限不显示操作按钮 |
| 回收站恢复/删除 | `canRestoreAsset` / `canPurgeAsset` | 无权限显示"-" |
| 查看器底部按钮 | `canManageAsset` / `canDownloadAsset` / `canCreateShare` | 无权限显示"申请编辑权限" |
| 查看器日志Tab | `canManageAsset` | 无权限隐藏日志标签 |
| 分享记录列表 | `canManageShare` | 无权限不显示"管理分享" |
| 收集任务列表 | `canManageCollect` | 无权限按钮显示"查看"而非"管理邀请" |
| 标签表格操作 | `canManageTag` | 无权限显示"-" |

---

## 三、素材收集完整业务与数据流程

### 3.1 收集任务创建流程

```
用户点击"创建收集任务"
    │
    ├── handleGroupAction("collect", groupId) → openCollectTaskModal(getGroupName(groupId))
    │   组菜单入口会传入组名
    │
    └── bindEvents: #collectCurrent → openCollectTaskModal(getGroupName(state.groupId))
        当前组入口同样传入组名
    │
    ▼
openCollectTaskModal()
    ├── 权限检查: canContributeToGroup(groupId)
    ├── 填充文件类型下拉 (getAllCollectTaskTypes)
    ├── 设置日期控件 (initProjectDatePickers)
    ├── 收集任务配置弹框使用 renderHtmlTemplate() 注入日期字段
    └── 显示弹窗
    │
    ▼
handleCollectTaskSubmit()
    ├── 收集表单数据 (name, desc, group, expiresAtDate, expiresAtTime, types)
    ├── 生成任务 ID: collect-{timestamp}
    ├── 生成访问码: 4位随机字符
    ├── 生成访问链接: /?collect={code}
    ├── 写入 db.collectTasks:
    │   { id, theme, desc, group, status:"生效中", code, creator,
    │     createdBy, ownedBy, ownedByDept, createdAt, updatedAt,
    │     expiresAt, requirePassword:true, password:code, types, link, logs }
    ├── logOperation('collect', ..., 'collect.create', ...)
    └── 跳转到收集任务列表页
```

### 3.2 外部收集上传流程

```
外部用户访问 /?collect={code}
    │
    ▼
renderCollectorPortal(code)
    ├── 查找 db.collectTasks 中 code 匹配的任务
    ├── 任务不存在 → 显示"收集链接无效"
    ├── normalizeCollectTaskSecurity(task) → 补齐密码字段
    ├── 任务状态检查
    └── 显示密码门 (renderCollectorPasswordGate)
    │
    ▼ 密码验证通过
renderCollectorPortalContent(code, task)
    ├── 显示上传表单
    │   字段: 提交人姓名、公司/部门、联系方式、素材说明、业务标签、文件
    ├── 上传区暂存文件并显示缩略图
    ├── 文件类型校验 (task.types)
    └── 表单提交:
    │
    ▼
createAssetFromFile(file, {
  auditStatus: pending_audit,
  assetStatus: pending,
  asset_source: "external",
  collect_id: task.id,
  collect_link: task.link,
  customTags: 业务标签
})
    ├── 生成素材 ID: asset-{timestamp}-{random}
    ├── AI标签识别 (recognizeTags)
    ├── 车型识别 (detectVehicleModel)
    ├── 计算 MD5/版本号
    └── 返回素材对象:
        { id, name, src, format, mime, type, sizeBytes, width, height,
          desc, brand, model, customTags, aiTags, groupId,
          owner: 提交人姓名, department: 公司/部门,
          creator, lastUpdate, createdBy, ownedBy, ownedByDept,
          asset_source: "external",
          collect_id: task.id, collect_link: task.link,
          permission: "企业内部 - 可下载",
          validUntil: 永久有效, validUntilDate,
          auditStatus: "pending_audit",
          assetStatus: "pending",
          uploadDate, validStart, version, logs:[] }
    │
    ▼
db.assets.unshift(asset)
logOperation('asset', ..., 'asset.upload', '通过收集链接上传素材')
saveDb()
    │
    ▼ 素材进入待入库列表，由 `isPendingAsset(asset)` 命中
```

### 3.3 内部上传流程

```
用户点击"上传" → handleFiles(fileList, options)
    │
    ├── 权限检查: canUploadToGroup(groupId)
    ├── 文件格式过滤 (isAllowedUploadFile)
    ├── 自动创建收集任务:
    │   db.collectTasks.unshift({ status:"已完成", ... })
    ├── logOperation('collect', ..., 'collect.create', ...)
    │
    ▼ 遍历文件
createAssetFromFile(file, {
  collect_id,
  collect_link,
  asset_source: "internal",
  auditStatus: pending_audit,
  assetStatus: pending
})
    ├── 默认也会落到待入库状态
    └── 写入素材对象
    │
    ▼
db.assets.unshift(asset)
logOperation('asset', ..., 'asset.upload', '上传了素材')
    │
    ▼
state.page = "pending"  // 跳转到待入库页
    └── getFilteredAssets() + isPendingAsset(asset) 显示刚上传素材
```

### 3.4 审核流程

```
收集任务列表页 (renderCollectPage)
    │
    ├── 过滤: db.collectTasks.filter(canManageCollect)
    ├── 查找任务关联素材: db.assets.filter(asset.collect_id === task.id)
    ├── 检查待机审素材: asset.auditStatus === pending_audit
    ├── 检查待人审素材: asset.auditStatus ∈ machine_pass / pending_human / human_auditing
    └── 显示按钮:
        ├── 有待机审素材 && canManage → "发起机审"
        └── 有待人审素材 && canManage → "发起审核"
    │
    ▼
simulateMachineAudit(taskId)
    ├── 查找 auditStatus=pending_audit 的素材
    ├── 设置 asset.auditStatus = machine_pass
    ├── 写入日志: { action:"asset.auto_audit", actionName:"机审通过", ... }
    └── saveDb()
    │
    ▼
simulateHumanAudit(taskId)
    ├── 查找待人审素材
    ├── 设置 asset.auditStatus = human_pass
    ├── 设置 asset.assetStatus = active
    ├── 设置 task.auditStatus = human_pass，task.status = completed
    ├── 写入日志: { action:"asset.human_audit", actionName:"人审通过", ... }
    └── saveDb()
```

### 3.5 收集任务数据结构

```javascript
{
  id: "collect-{timestamp}",
  theme: "任务名称",
  desc: "任务描述",
  group: "存放素材组名",
  status: "生效中" | "已失效" | "已完成" | "已关闭",
  code: "4位访问码",
  creator: "创建人名",
  createdBy: "创建人名",
  ownedBy: "责任人名",
  ownedByDept: "责任部门",
  createdAt: "2026/07/09 14:00",
  updatedAt: "2026/07/09 14:00",
  expiresAt: "2026/08/09 14:00" | "永久有效",
  requirePassword: true | false,
  password: "访问密码",
  types: ["图片", "视频", ...],
  link: "/?collect=xxxx",
  logs: [{ operator, operatorDept, action, actionName, detail, createdAt }]
}
```

---

## 四、素材分享完整业务与数据流程

### 4.1 分享创建流程

```
                    ┌─────────────────────┐
                    │   三种分享入口        │
                    └─────┬───────┬───────┘
                          │       │       │
              ┌───────────┘       │       └───────────┐
              ▼                   ▼                   ▼
    openShareCurrentModal()  openShareAssetModal()  openBasketShareModal()
    (素材组分享)               (单素材分享)            (素材篮分享)
              │                   │                   │
              │   ┌───────────────┘                   │
              │   │                                   │
              ▼   ▼                                   ▼
    ┌─────────────────────────────────────────────────────────┐
    │  权限检查                                                 │
    │  canCreateShare(target, contentPermission)               │
    │  ├─ admin → true                                         │
    │  ├─ group → canViewGroup / canDownloadFromGroup          │
    │  └─ asset → canViewAsset / canDownloadAsset              │
    └─────────────────────┬───────────────────────────────────┘
                          │
                          ▼
    ┌─────────────────────────────────────────────────────────┐
    │  初始化弹窗                                               │
    │  ├── populateShareDropdowns()  → 从值列表树填充下拉      │
    │  │   ├── 访问范围 (vl_dim_share_scope)                   │
    │  │   │   ├─ internal: 仅公司内部                          │
    │  │   │   └─ public: 公开互联网                           │
    │  │   └── 内容权限 (vl_dim_share_perm)                    │
    │  │       ├─ view: 仅预览                                 │
    │  │       └─ download: 可下载                              │
    │  ├── bindShareExpireButtons()  → 有效期按钮 (7/30/90/永久) │
    │  ├── bindSharePasswordToggle() → 密码开关                │
    │  ├── bindShareAccessScopeChange() → 范围变更提示         │
    │  ├── updateShareContentPermissionOptions() → 内容权限联动 │
    │  │   (无下载权限时禁用 download 选项)                     │
    │  └── updateShareAccessScopeOptions() → 范围联动          │
    │      (非管理员禁用 public 选项)                           │
    └─────────────────────┬───────────────────────────────────┘
                          │
                          ▼
    ┌─────────────────────────────────────────────────────────┐
    │  getShareFormData() 收集表单                              │
    │  ├── accessScope: internal / public                      │
    │  ├── contentPermission: view / download                  │
    │  ├── expiresAt: "2026/08/09 14:00" 或 "永久有效"          │
    │  ├── requirePassword: true/false                         │
    │  ├── password: 密码明文                                   │
    │  ├── maxVisits: 数字 或 null                             │
    │  └── shareName: 分享名称                                 │
    └─────────────────────┬───────────────────────────────────┘
                          │
                          ▼
    ┌─────────────────────────────────────────────────────────┐
    │  写入 db.shares                                           │
    │  { id, group, user, access, visits:0, views:0,          │
    │    downloads:0, sharedAt, expiresAt, targetType,        │
    │    targetId, code, link, requirePassword, password,     │
    │    accessScope, contentPermission, maxVisits,           │
    │    status:"active", ownedBy, ownedByDept, createdBy,     │
    │    updatedAt, logs:[] }                                   │
    │                                                          │
    │  logOperation('share', ..., 'share.create', ...)         │
    └─────────────────────────────────────────────────────────┘
```

### 4.2 分享访问流程（外部门户）

```
用户访问分享链接 /?share=group:versailles:abc123
    │
    ▼
renderSharePortal(token)
    ├── 解析 token: [targetType, targetId, code]
    ├── 查找 db.shares 匹配的分享记录
    ├── 分享不存在 → renderShareLandingError("分享链接无效")
    ├── normalizeShareSecurity(share) → 补齐字段
    ├── 分享状态检查: share.status !== "active" → "分享已失效"
    ├── 过期检查: isShareExpired(share) → "分享已过期"
    ├── 访问次数检查: share.visits >= share.maxVisits → "访问次数已达上限"
    ├── 内部范围检查:
    │   accessScope === "internal"
    │   ├── 未登录 → renderShareLandingLogin() → 登录后重新进入
    │   │   登录成功后重建 currentUser 并调用 buildUserGroupPermissionCache()
    │   └── 部门不匹配 → "无权访问"
    └── 密码检查:
        requirePassword && password && sessionStorage 不匹配
        → renderSharePasswordGate() → 密码验证后进入
    │
    ▼ 所有检查通过
renderSharePortalContent(token, share)
    ├── 计算 nextVisits = share.visits + 1
    ├── nextVisits > maxVisits → status="expired" 并拦截
    ├── 未超限时写入 share.visits = nextVisits
    ├── saveDb()
    ├── 获取素材列表: getSharePortalAssets(targetType, targetId)
    │   ├── group: db.assets.filter(groupId === targetId)
    │   ├── asset: 查找单个素材
    │   └── basket: 按 targetId 拆分查找多个素材
    ├── 判断内容权限: canDownload = contentPermission === "download"
    └── 渲染分享页面:
        ├── 下载工具栏: canDownload ? 显示 : hidden
        ├── 素材卡片:
        │   ├── checkbox: canDownload ? 可选 : disabled
        │   └── 下载链接: canDownload ? 显示 : 隐藏
        ├── 批量下载: canDownload ? 可用 : 隐藏
        └── 一键全部下载: canDownload ? 可用 : 隐藏
```

### 4.3 分享管理流程

```
分享记录页 (renderSharePage)
    │
    ├── 过滤: db.shares.filter(canManageShare)
    │   ├── admin → 全部可见
    │   ├── share.ownedBy === currentUser.name → 可见
    │   └── targetType=group → canManageGroup(targetId)
    │       targetType=asset → canManageAsset(...)
    │
    ├── 表格列: 素材组 | 分享人 | 链接权限 | 访问 | 浏览 | 下载 |
    │          分享时间 | 过期时间 | 状态 | 操作
    │
    └── 操作按钮: canManageShare ? "管理分享" : ""
    │
    ▼
openShareRecordConfigModal(index)
    ├── 权限检查: canManageShare(share)
    ├── 显示配置弹窗 (tplShareRecordConfigForm)
    │   字段: 访问密码、有效期、访问范围、内容权限
    └── 提交修改:
        ├── Object.assign(share, security)
        ├── logOperation('share', ..., 'share.update', ...)
        └── saveDb()
```

### 4.4 分享数据结构

```javascript
{
  id: "share-{timestamp}",
  group: "素材组/素材名称",
  user: "分享人名",
  access: "分享名称",
  visits: 0,           // 访问人数
  views: 0,            // 浏览次数
  downloads: 0,        // 下载数量
  sharedAt: "2026/07/09 14:00",
  expiresAt: "2026/08/09 14:00" | "永久有效",
  targetType: "group" | "asset" | "basket",
  targetId: "组ID | 素材ID | 逗号分隔的素材ID列表",
  code: "6位随机码",
  link: "/?share=group:versailles:abc123",
  requirePassword: true | false,
  password: "访问密码明文",
  accessScope: "internal" | "public",
  contentPermission: "view" | "download",
  maxVisits: null | 数字,
  status: "active" | "revoked" | "expired",
  ownedBy: "创建人名",
  ownedByDept: "部门",
  createdBy: "创建人名",
  updatedAt: "更新时间",
  logs: [{ operator, operatorDept, action, actionName, detail, createdAt }]
}
```

### 4.5 分享的安全控制层次

```
第1层: 创建权限 (canCreateShare)
  └── 创建者必须有素材/组的查看或下载权限

第2层: 访问范围 (accessScope)
  ├── internal: 需要登录验证 + 部门匹配
  └── public: 任何人可访问

第3层: 访问密码 (requirePassword + password)
  └── 需要输入正确密码才能查看内容

第4层: 有效期 (expiresAt)
  └── 超过有效期自动失效

第5层: 最大访问次数 (maxVisits)
  └── 达到上限后自动失效

第6层: 内容权限 (contentPermission)
  ├── view: 只能预览，不能下载
  └── download: 可以预览和下载

第7层: 管理权限 (canManageShare)
  └── 只有管理员/所有者/组管理员可修改配置
```

---

## 五、操作日志体系

### 5.1 日志数据结构

```javascript
// 实体级日志 (asset.logs / group.logs / tag.logs / share.logs / collect.logs)
{
  operator: "操作人名",
  operatorDept: "操作人部门",
  action: "asset.upload",       // 操作类型 code
  actionName: "上传素材",        // 操作类型中文名
  detail: "上传了素材 \"xxx\"",  // 操作详情
  createdAt: "2026/07/09 14:00"
}

// 全局日志 (db.operationLogs)
{
  id: "log-{timestamp}-{random}",
  operator, operatorDept, action, actionName, detail, createdAt,
  targetType: "asset" | "group" | "tag" | "share" | "collect" | "acl",
  targetId: "目标ID",
  targetName: "目标名称",
  ip: ""
}
```

### 5.2 操作类型完整清单

| code | 名称 | 场景 |
|------|------|------|
| asset.upload | 素材上传 | 内部上传/外部收集上传 |
| asset.edit | 素材编辑 | 修改素材信息/有效期/审核入库 |
| asset.delete | 素材删除 | 软删除/彻底删除 |
| asset.restore | 素材恢复 | 从回收站恢复 |
| asset.move | 素材移动 | 素材组间移动/组解散 |
| asset.download | 素材下载 | 下载原文件 |
| asset.tag | 素材打标签 | 标签修改/AI重识别 |
| asset.view | 素材浏览 | 查看器打开 |
| asset.comment | 素材评论 | 发布评论 |
| asset.auto_audit | 机审通过 | 模拟机审 |
| asset.human_audit | 人审通过 | 模拟人审 |
| group.acl.add | 组权限添加 | 添加ACL |
| group.acl.remove | 组权限移除 | 移除ACL |
| group.acl.update | 组权限更新 | 修改ACL |
| group.owner.transfer | 组所有权转移 | 责任人交接 |
| group.create | 组创建 | 新建素材组 |
| group.rename | 组重命名 | 修改组名 |
| group.move | 组移动 | 调整层级 |
| group.delete | 组删除 | 删除/解散 |
| asset.acl.add | 素材权限添加 | 添加素材ACL |
| asset.acl.remove | 素材权限移除 | 移除素材ACL |
| share.create | 分享创建 | 创建分享链接 |
| share.revoke | 分享撤销 | 失效分享链接 |
| share.update | 分享更新 | 修改分享配置 |
| collect.create | 收集任务创建 | 创建收集任务 |
| collect.close | 收集任务关闭 | 关闭收集任务 |
| request.submit | 权限申请提交 | 用户申请权限 |
| request.approve | 权限申请通过 | 管理员通过申请 |
| request.reject | 权限申请拒绝 | 管理员拒绝申请 |
| tag.create | 标签创建 | 新建标签 |
| tag.edit | 标签编辑 | 修改/重命名标签 |
| tag.delete | 标签删除 | 删除标签 |

---

## 六、当前结论与维护重点

### 6.1 权限体系当前状态

- 菜单权限、素材组 ACL、素材 ACL、权限申请审批、分享权限、收集任务权限已经形成前端闭环。
- 组权限采用继承模型，素材权限采用“组权限优先 + 素材独立授权补充”的模型。
- 权限申请页已经可以审批申请，审批结果会回写到组 ACL 或素材 ACL，并避免重复审批产生重复授权。
- ACL 主体支持人员、部门和全公司，部门授权可按 `includeSubDept` 匹配下级组织。

### 6.2 业务流程当前状态

- 内部上传和外部收集上传都先进入待入库链路，入库口径由 `assetStatus` 与 `auditStatus` 共同决定。
- 收集任务落地页具备密码门、素材暂存、缩略图预览、提交人信息、业务标签和提交入待入库能力。
- 分享落地页具备密码门、内外部访问范围、内容权限、访问次数和过期状态控制。
- 值列表是权限枚举、素材状态、审核状态、分享状态和业务标签等选项的统一来源。

### 6.3 后续维护重点

- 前端权限只适合作为交互边界；一旦接入真实后端，需要在服务端复刻菜单权限、ACL 匹配、待入库判断和审批状态保护。
- 旧兼容函数与模板可以继续分批清理，但要先确认是否仍被动态模板、历史链接或测试入口引用。
- 菜单和值列表都已支持动态维护，后续新增页面或枚举时应优先走现有管理入口，避免重新写死常量。
